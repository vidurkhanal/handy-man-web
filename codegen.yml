overwrite: true
schema: "http://localhost:8080"
documents: "src/**/*.gql"
generates:
  generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
    config:
      constEnums: true
      immutableTypes: true
